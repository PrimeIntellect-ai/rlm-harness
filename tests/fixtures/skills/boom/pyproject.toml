[project]
name = "rlm-skill-boom"
version = "0.1.0"
description = "Always raises (test skill)."
requires-python = ">=3.10"

[project.scripts]
boom = "boom.boom:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/boom"]
