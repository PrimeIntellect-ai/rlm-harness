[project]
name = "rlm-skill-say"
version = "0.1.0"
description = "Print a string (test skill)."
requires-python = ">=3.10"

[project.scripts]
say = "say.say:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/say"]
