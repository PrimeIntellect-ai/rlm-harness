[project]
name = "rlm-skill-say"
version = "0.1.0"
description = "Echo a string (test skill)."
requires-python = ">=3.10"

[project.scripts]
say = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/say"]
