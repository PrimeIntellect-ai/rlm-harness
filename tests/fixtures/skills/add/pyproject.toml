[project]
name = "rlm-skill-add"
version = "0.1.0"
description = "Add two integers (test skill)."
requires-python = ">=3.10"

[project.scripts]
add = "add.add:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/add"]
